{
  "name": "han-georeum-sanchaek",
  "version": "1.0.0",
  "private": true,
  "description": "문장 패턴 드릴 웹앱 — 한 걸음 산책",
  "type": "module",
  "scripts": {
    "gen": "node scripts/generate-data.mjs",
    "test": "npm run gen && node --test",
    "build": "npm run gen && node build.mjs",
    "dev": "npm run gen && node build.mjs --serve"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
